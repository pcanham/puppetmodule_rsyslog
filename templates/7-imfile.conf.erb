module(load="imfile" PollingInterval="10") #needs to be done just once

input(type="imfile" File="<%= scope.lookupvar('logfilename') %>" 
      Tag="<%= scope.lookupvar('filetag') %>" 
      StateFile="<%= scope.lookupvar('statefile') %>" 
      Severity="<%= scope.lookupvar('severity') %>" 
      Facility="<%= scope.lookupvar('factility') %>")
      