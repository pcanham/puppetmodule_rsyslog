module(load="imfile" PollingInterval="10") #needs to be done just once

input(type="imfile" File="<%= @logfilename %>" 
      Tag="<%= @filetag %>" 
      StateFile="<%= @statefile %>" 
      Severity="<%= @severity %>" 
      Facility="<%= @factility %>")
